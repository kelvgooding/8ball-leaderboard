CREATE TABLE recently (
    p1_name     VARCHAR (50),
    p1_result   INT,
    p2_name     VARCHAR (50),
    p2_result   INT,
    date_played DATE
);

CREATE TABLE leaderboard (
    players    VARCHAR (50),
    total_wins INT
);